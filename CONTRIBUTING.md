# Contributing

## Commit Messages

Commits must follow [Conventional Commits](https://www.conventionalcommits.org/).

Use a scope when possible to make the change area explicit.

Preferred format:

`type(scope): short description`

Example:

`fix(parser): handle empty input`
